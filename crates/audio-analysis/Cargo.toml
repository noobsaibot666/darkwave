[package]
name = "audio-analysis"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
audio-metadata = { path = "../audio-metadata" }
# Pinned to exactly what voice_activity_detector already locks, so the two
# ONNX Runtime consumers unify onto one linked runtime rather than fighting.
ort = "=2.0.0-rc.10"
pitch-detection = "0.3.0"
rustfft = "6"
voice_activity_detector = "0.2"
